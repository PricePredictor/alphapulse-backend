services:
  backend:
    build: .
    container_name: alphapulse-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app        # ✅ mount whole project folder, not just ./app
    env_file:
      - .env
